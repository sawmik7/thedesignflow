# General AI Instructions for Claude / Antigravity

This repository is governed strictly by the rules outlined in `AGENTS.md`. 
**DO NOT** proceed with coding tasks without first reading `AGENTS.md` and utilizing the global skills stored in your environment.

## Elite Agent Directives:
1. **Always use your Skills**: You are equipped with a suite of highly refined skills (e.g., `ui-ux-pro-max`, `elite-frontend-design`, `comprehensive-code-review`, `agent-superpower`, combined globally with `animation-expert`). Apply them to your daily workflow.
2. **Never output generic interfaces**: The expectation for this project is "museum-quality" design. Use the Design System Generator logic before coding.
3. **Data-backed Content**: Use the `content-research-writer` and `product-research-analysis` frameworks before inserting placeholder copy into the website.
4. **Auto-Orchestration**: The multi-agent systems (`oh-my-claudecode` orchestrations, `agent-manager-skill`, etc.) MUST start automatically whenever a process is complex or needs sub-task delegation, executing silently alongside standard agents.

Refer to `AGENTS.md` for strict Next.js 15, React 19, and Tailwind 4.0 guidelines.
